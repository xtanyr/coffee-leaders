PORT=3011

# Comma-separated browser origins (must match how users open the frontend)
# CORS_ORIGIN=http://localhost:3100

# Database (Prisma) — set in your real .env, not committed
# DATABASE_URL="file:./prisma/dev.db"
